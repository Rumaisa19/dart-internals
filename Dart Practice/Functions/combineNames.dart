// Function combineNames(String first, String last) → "first last"

String combineNames(String firstPart, String lastPart) =>
    "$firstPart $lastPart";
void main() {
  print(combineNames('Rumaisa', "Mushtaq"));
}
